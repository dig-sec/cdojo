# Based on https://github.com/jborean93/ansible-windows/blob/master/vagrant/inventory.yml
# Images https://app.vagrantup.com/boxes/search?utf8=%E2%9C%93&sort=downloads&provider=libvirt&q=windows+2022
all:
  hosts:
    dc01:
      ip: 10.0.1.10
      vagrant_box: jborean93/WindowsServer2019
      box_type: windows
      linked_clone: true
      cpus: 2
      memory: 4096
    # win2019-adcs:
    #   ip: 10.0.1.14
    #   vagrant_box: jborean93/WindowsServer2019
    #   box_type: windows
    #   linked_clone: true
    # # win10-client:
    # #   ip: 10.0.1.25
    # #   vagrant_box: peru/windows-10-enterprise-x64-eval
    # #   box_type: windows
    # #   linked_clone: true
    # # win2022-1:
    # #   ip: 10.0.1.17
    # #   vagrant_box: jborean93/WindowsServer2022
    # #   box_type: windows
    # #   linked_clone: true
    # win11-1:
    #   ip: 10.0.1.22
    #   vagrant_box: valengus/windows11-22h2-x64
    #   box_version: 2021.12.09
    #   box_type: windows
    #   linked_clone: true
    # metasploitable3-win2k8:
    #   ip: 10.0.1.30
    #   vagrant_box: tmarchst/metasploitable3-win2k8
    #   box_type: windows
    # metasploitable3-ub1404:
    #   ip: 10.0.1.31
    #   vagrant_box: tmarchst/metasploitable3-ub1404
    #   box_type: linux
    # ubuntu2004:
    #   ip: 10.0.1.34
    #   vagrant_box: ubuntu/focal64
    #   box_version: 20221103.0.0
    #   box_type: linux
  vars:
    root_domain: cyberrange.local
    domain_name: cyberrange
    win_domain_admin: Administrator
    win_domain_admin_pass: P@ssw0rd
    adcs_host: win2019-adcs
    adcs_common_name: cyberrange-win2019-adcs-CA

    # ansible winrm config
    ansible_winrm_server_cert_validation: ignore
    ansible_winrm_scheme: http

    # these are default values for hosts, you can override them in the host-specific config above
    default_host:
      cpus: 1
      memory: 2048

    # each protocol and host have their own port forward
    # the starting IP range (in thousands) is the 'host' variable
    # we add the host portion of the IP address to this e.g. dc01: rdp port is 20010, winrm is 22010, etc
    # we are theoretically maxed at a single network since a .10 on one network would clash with a .10 on another
    vagrant_ports:
      rdp:
        guest: 3389
        host: 30000
      ssh:
        guest: 22
        host: 31000
      winrm_http:
        guest: 5985
        host: 32000
      winrm_https:
        guest: 5986
        host: 33000
      smb:
        guest: 445
        host: 34000